extends layout

block content
  table(border='1')
    tr
      th 姓名
      th 邮箱
    each user in users.length ? users : ['There are no users']
      tr
        td
          a(href='/users/#{user._id}') #{user.name}
        td= user.email